server:
  port: 8082

spring:
  application:
    name: Consumer
  data:
    mongodb:
      # Removed the placeholder to be 100% sure it uses this URI
      uri: mongodb://localhost:27017/texttolearn
      # Explicitly setting the database name as a separate property
      database: texttolearn
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

rabbitmq:
  exchange: texttolearn.exchange
  queue: texttolearn.queue
  routing-key: "topic.#"
  persistence:
    queue: texttolearn.persistence.queue
    routing-key: "persistence.#"
